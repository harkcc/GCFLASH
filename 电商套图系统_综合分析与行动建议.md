# AI 电商套图生成系统 — 综合分析与首步行动建议

**基于 PRD v1.0 + 行业调研 + 技术评估**
**日期：2026-04-10**

---

## 一、PRD 整体评价

文档质量很高，四层架构（策划→生图→渲染→质检）逻辑清晰，成本测算务实。几个值得肯定的设计决策：

- **AI 决定内容，代码决定布局**：这是目前业界最靠谱的路径，避免让 AI 直接控制排版
- **模板与数据分离**：HTML/CSS 模板 + JSON 变量，扩展性好
- **多模型路由**：不同图片类型用不同模型，而不是一把梭

需要补充和调整的地方我在下面展开。

---

## 二、电商图片分类体系建议

PRD 中定义了 7 张图、5 种类型（hero/scene/feature/detail/size_ref）。结合行业调研，建议调整为以下 **8 类标准图片**，实际每套选 7-9 张：

### 推荐分类

| 序号 | 图片类型 | 英文标识 | 说明 | 是否必须 |
|------|---------|---------|------|---------|
| 1 | **主图（白底）** | `hero_white` | 纯白背景，产品正面，无文字无logo。Ozon强制要求主图白底 | ✅ 必须 |
| 2 | **主图（场景版）** | `hero_scene` | 产品在使用场景中的英雄展示，用于吸引点击 | ✅ 必须 |
| 3 | **使用场景图** | `lifestyle` | 产品在真实生活场景中的展示（厨房、户外等），提升转化率 ~22% | ✅ 必须 |
| 4 | **功能卖点图** | `feature` | 图文结合，展示2-4个核心功能/卖点，带英文标注 | ✅ 必须 |
| 5 | **产品细节图** | `detail` | 材质、工艺、接口等细节特写 | ✅ 必须 |
| 6 | **参数规格图** | `specs` | 尺寸标注、参数列表、材质说明等信息图 | ✅ 必须 |
| 7 | **尺寸参考图** | `size_ref` | 与人/手/常见物品的尺寸对比 | 推荐 |
| 8 | **多角度图** | `multi_angle` | 正面/侧面/背面/俯视等不同角度展示 | 推荐 |

### 与 PRD 的差异

1. **拆分了两种主图**：白底主图是 Ozon 硬性要求（主图不允许文字/logo），场景版主图用于信息流吸引点击，两者目的完全不同
2. **新增多角度图**：电子产品、家居用品的多角度展示对降低退货率很有效
3. **scene → lifestyle**：更精准的命名，强调"生活方式"而非泛泛的"场景"

### 按品类的侧重建议

| 品类 | 重点图片 | 说明 |
|------|---------|------|
| 电子产品 | specs + detail + multi_angle | 参数是决策关键，接口细节很重要 |
| 家居厨房 | lifestyle + feature + size_ref | 场景感最重要，尺寸避免退货 |
| 户外运动 | lifestyle + feature + detail | 户外真实场景 + 功能演示 |
| 美容个护 | detail + lifestyle + feature | 质感和成分展示，使用场景 |
| 汽车配件 | specs + detail + size_ref | 兼容性参数、安装细节 |

### 关于模板和垫图

- **模板**：前期建议做 4 个核心模板就够用——`hero_clean`（纯净展示）、`feature_grid`（功能卖点网格）、`specs_panel`（参数面板）、`comparison`（对比图）。通用模板优先，品类专属模板后续按需加
- **垫图**：建议收集每个品类 10-15 张高质量背景/场景垫图，作为 IP-Adapter 的参考素材和模板背景素材
- **品类设计规范**：前期不需要每个品类单独设计，用统一的视觉风格 + 品类配色方案即可。后续根据数据反馈再做品类定制

---

## 三、技术框架选型评估

### 3.1 Agent 框架对比

| 框架 | 可控性 | 调试 | 生态 | 图像工作流适配 | 建议 |
|------|-------|------|------|-------------|------|
| **LangGraph** | ⭐⭐⭐⭐⭐ | 时间旅行调试、检查点 | 最成熟 | 好，支持复杂状态机 | **首选** |
| **Claude Agent SDK** | ⭐⭐⭐⭐ | MCP 工具链 | MCP 生态独特 | 好，原生多模态 | 备选 |
| **Google ADK** | ⭐⭐⭐⭐ | 模块化 | 内置 Gemini | 好，内置图像能力 | 值得测试 |
| **OpenAI Agents SDK** | ⭐⭐⭐ | 简洁 | OpenAI 生态 | 一般 | 不推荐 |
| **自建框架（TS）** | ⭐⭐⭐⭐⭐ | 完全自主 | 需自建 | 完全定制 | 长期选项 |

### 3.2 我的建议

**短期（Phase 0-1）：先不用框架，用"胶水代码"验证效果。**

理由很简单：你的核心流程其实是一个确定性的 pipeline（分析→生图→渲染→质检），不是一个需要复杂决策树的 Agent。前期用 TypeScript 写几个函数串起来就够了，把精力花在 prompt 调优和模型效果上。

**中期（Phase 2-3）：引入 LangGraph 或你自己的框架。**

当你需要处理以下场景时再引入框架：
- 复杂的重试和回退逻辑（某张图反复失败，需要换策略）
- 并发调度多个 SKU 的任务队列
- 人工审核的状态管理（暂停→人工介入→继续）

LangGraph 的优势在于它的状态持久化和检查点能力，这在批量处理中很有价值。但如果你自己的框架已经在搭了，完全可以先用自己的，等遇到具体痛点再评估是否切换。

### 3.3 AI 生图模型建议

PRD 中的模型选择基本合理，补充几点：

- **Flux 2 Pro** 仍然是产品摄影真实感的最佳选择
- **Ideogram 3.0** 在英文文字渲染上确实领先，但如果走 HTML 模板合成路径，文字准确率就是 100%，可以减少对 Ideogram 的依赖
- **LoRA 训练**已经成熟，20-30 张图 + 2500-3000 步就能产出可用的风格 LoRA
- **fal.ai 的替代品**：Pixazo（成本低 30-50%）和 WaveSpeedAI 值得关注，但 fal.ai 在模型丰富度和 SDK 成熟度上仍然领先
- **图像编辑模型**（inpainting/outpainting/背景替换）在 2026 年已经很成熟，可以用来做产品图的背景替换和场景扩展

---

## 四、首步行动建议

鉴于你前期允许烧钱、优先验证效果，我建议跳过 PRD 中的 Phase 0 里"不写代码"的限制，**直接边测边写**：

### 第一步：立即可做（本周）

**1. fal.ai 账号注册 + 充值 + Playground 测试**
- 充 $20-50，测试 Flux 2 Pro / Ideogram 3.0 / Flux Schnell
- 用你自己的产品白底图，测试不同 prompt 风格
- 重点验证：产品是否变形、场景是否自然、英文文字是否准确

**2. 我帮你搭建 Phase 0 验证脚本**
- 一个简单的 TypeScript 脚本，输入白底图 + 产品信息 JSON → 调用 fal.ai → 输出 7 张图
- 包含基本的 prompt 模板和模型路由
- 不需要完整框架，胶水代码即可

**3. HTML 模板原型**
- 我可以先做 2-3 个 HTML/CSS 模板（feature_grid、specs_panel）
- 用 Playwright 截图验证效果

### 第二步：效果验证（1-2 周）

**4. 选 3-5 个真实 SKU 端到端跑一遍**
- 覆盖不同品类（电子、家居、户外）
- 记录每张图的生成效果、失败模式
- 你做人工评审，给每张图打分

**5. LoRA 训练测试**
- 收集 15-20 张 Ozon 平台优质主图
- 在 fal.ai 上训练一个 ozon_style LoRA
- 对比有/无 LoRA 的效果差异

### 第三步：飞书集成（并行）

**6. 飞书多维表格结构设计**
- 设计表格字段：SKU ID、产品名称、白底图、生成状态、7 张图预览、审核状态
- 简单的 AIGC 功能（如白底图生成）可以直接在飞书上用 AI 辅助

---

## 五、我现在能帮你做的事

1. **写 Phase 0 验证脚本**（TypeScript，调用 fal.ai API）— 你提供 API key 就能跑
2. **设计 HTML/CSS 模板**（feature_grid、specs_panel 等）— 我可以直接写
3. **搭建 prompt 模板库** — 按品类和图片类型组织的 prompt 模板
4. **飞书多维表格字段设计方案**
5. **分析你提供的竞品图片** — 你后续发给我好看的图片，我来分析风格特征

你觉得从哪个开始？或者你想先讨论哪个部分的细节？
