# Excitat 电商套图 AI 生成系统 — 全景概览

**版本：v0.3 | 更新：2026-04-12**

---

## 一、生图模型矩阵

### 1. Flux 系列（Black Forest Labs）

| 型号 | 端点 | 核心优势 | 核心劣势 | 适用场景 | 单价 |
|------|------|---------|---------|---------|------|
| **Flux 2 Pro** | `fal-ai/flux-2-pro` | 2026年写实天花板，皮肤纹理/光影/自然姿态最逼真，色彩精准支持HEX指定，最高4MP | 文字渲染弱，不擅长精确布局控制 | Hero主图、Lifestyle场景图、Detail细节图 — 凡是需要"像真实相机拍的"都用它 | ~$0.03/MP |
| **Flux 1.1 Pro** | `fal-ai/flux-pro/v1.1` | 成熟稳定，社区资源多，效果接近Flux 2但稍逊 | 被Flux 2逐步替代 | 作为Flux 2的fallback，或者对比测试 | ~$0.05/张 |
| **Flux 1.1 Ultra** | `fal-ai/flux-pro/v1.1-ultra` | 更高分辨率，细节更丰富 | 价格较高 | Hero主图需要极致细节时使用 | ~$0.08/张 |
| **Flux Schnell** | `fal-ai/flux/schnell` | 极快（<1秒），免费/极低成本 | 质量明显低于Pro | 快速草稿预览、prompt调试、批量初筛 | 几乎免费 |
| **Flux LoRA** | `fal-ai/flux-lora` | 支持自定义LoRA，可训练品牌风格 | 需要训练数据和时间 | 品牌框风格训练、特定产品风格一致性 | 训练~$3-5 |
| **Flux Redux** | `fal-ai/flux-pro/v1.1/redux` | 图生图变体，保持参考图风格 | 对参考图的理解有偏差 | 基于现有图片生成风格变体 | ~$0.05/张 |

**总结：Flux 系列 = 写实之王。产品图、场景图、细节图首选 Flux 2 Pro。**

---

### 2. Ideogram V3

| 维度 | 说明 |
|------|------|
| **端点** | `fal-ai/ideogram/v3` |
| **核心优势** | 文字渲染准确率~90%（业界最高），文字能与画面构图融合而非简单叠加，排版设计感强 |
| **核心劣势** | 写实感不如Flux 2，人物/场景的自然度稍差，风格偏"设计感"而非"摄影感" |
| **适用场景** | Benefit卖点图（需要大标题）、Specs参数图（需要图标+文字）、任何需要图内文字的图 |
| **单价** | ~$0.08/张 |

**总结：Ideogram = 文字之王。凡是图上需要出现英文标题/说明文字的，优先用 Ideogram。**

---

### 3. Qwen-Image / 通义万相（阿里巴巴）

| 维度 | 说明 |
|------|------|
| **端点** | DashScope API: `wanx2.1-t2i-turbo` |
| **核心优势** | 基于自回归LLM架构（非扩散模型），多语言文字渲染强（中英俄），文字能适应曲面/材质（玻璃、布料、标牌），价格便宜 |
| **核心劣势** | 整体画面质量不如Flux 2，写实度中等，需要国内直连网络（阿里云域名需走直连不走代理） |
| **适用场景** | 需要中文/俄语文字的图、针对Ozon俄语市场的图、低成本批量初稿 |
| **单价** | ~¥0.14/张 |
| **网络要求** | 阿里云域名需加入Clash Verge直连规则（aliyuncs.com, aliyun.com 等走DIRECT） |

**总结：Qwen = 多语言文字 + 低成本。Ozon俄语市场图、中文版图首选。**

---

### 4. Nano Banana 2 / Gemini 3.1 Flash Image（Google）

| 维度 | 说明 |
|------|------|
| **端点** | Gemini API: `gemini-3.1-flash-image`（通过 Google AI Studio / Vertex API） |
| **核心优势** | 速度极快（Flash级别），文字渲染精准度高，支持512px到4K分辨率和多种宽高比，与Gemini生态无缝集成（可以先让Gemini理解产品再直接出图），SynthID水印内置 |
| **核心劣势** | 2026年2月刚发布，API可能还在preview阶段，社区资源和工作流生态不如Flux/ComfyUI成熟 |
| **适用场景** | 快速原型（比Schnell质量高很多但同样快），文字+画面一体生成，与Gemini规划层直接联动（Gemini生方案→直接出图，不用切换模型） |
| **单价** | 按Gemini API token计费，预计比Flux便宜 |
| **独特价值** | 它是唯一一个"理解力+生图力"合一的模型 — Gemini既能看懂产品信息做规划，又能直接生成图片，省去了"规划层→生成层"之间的prompt翻译损耗 |

**总结：Nano = 速度+理解力。适合快速迭代和Gemini一体化工作流。Phase 0 值得一试。**

---

### 5. 模型路由策略（按图片类型选模型）

```
每张图进入生成时，按这个逻辑选模型：

Hero主图        → Flux 2 Pro（写实第一）
Lifestyle场景图  → Flux 2 Pro（人物+场景自然度）
Benefit卖点图   → Ideogram V3（需要大标题文字） / Nano（快速带文字）
Detail细节图    → Flux 2 Pro（微距质感）
Specs参数图     → Ideogram V3（文字+图标密集） / HTML模板合成
Differentiator  → Flux 2 Pro 生场景 + 模板拼接
Trust信任图     → Flux 2 Pro（情感场景）

多语言版本      → Qwen-Image（中文/俄语）
快速草稿/调试    → Flux Schnell 或 Nano
品牌框生成      → AI直接生成（含在主图prompt中） / 后续LoRA训练

每张图同时跑 2-3 个模型 → 评审选最优
```

---

## 二、图片分类体系

### 7张套图的分类与设计规范

| 序号 | 类型 | 心理钩子 | 设计重点 | 推荐模型 | 品牌框 |
|------|------|---------|---------|---------|-------|
| 1 | **Hero 主图** | "这是什么？看起来很高级" | 产品主体、材质光影、干净构图 | Flux 2 Pro | AI生成，配色跟随产品主色调 |
| 2 | **Lifestyle 场景图** | "我能想象自己在用它" | 真实使用场景、人物、情感共鸣 | Flux 2 Pro | AI生成，边框色跟场景氛围 |
| 3 | **Benefit 卖点图** | "它能解决我的问题" | 核心功能可视化、动态效果 | Ideogram V3 / Nano | 文字融入画面 |
| 4 | **Detail 细节图** | "做工很扎实" | 微距材质、工艺特写、水滴等 | Flux 2 Pro | 简约框或无框 |
| 5 | **Specs 参数图** | "参数符合我的需求" | 产品全貌、尺寸标注、图标 | Ideogram V3 + HTML模板 | 图标条+品牌框 |
| 6 | **Differentiator 差异化图** | "比其他竞品更好" | 多场景拼接、独特卖点 | Flux 2 Pro + 模板拼接 | 需要竞品信息 |
| 7 | **Trust 信任图** | "别人也说好" | 包装开箱、礼物场景、评价 | Flux 2 Pro | 温馨氛围 |

### 每张图的 Prompt 结构格式

```json
{
  "image_number": 1,
  "image_type": "hero",
  "psychological_hook": "买家看到这张图时的内心想法",

  "headline_text": "主标题（3-6词英文）",
  "subtext": "副标题（8-15词英文）",

  "prompt_flux": "Flux专用prompt：专注写实画面，不含文字指令...",
  "prompt_ideogram": "Ideogram专用prompt：含文字渲染指令...",
  "prompt_qwen": "Qwen专用prompt：可含多语言文字...",
  "prompt_nano": "Nano专用prompt：理解力+文字一体...",
  "negative_prompt": "排除项...",

  "brand_frame_instruction": "品牌框指令：颜色跟随产品主色调，EXCITAT右上角...",
  "style_consistency_anchors": ["暗色背景", "RGB光晕", "白色粗体标题"],

  "generation_strategy": "flux2_pro + ideogram_text",
  "reference_images": ["可选：用户上传的实拍图作为参考"],

  "quality_targets": {
    "min_score": 7,
    "key_criteria": ["product_accuracy", "mood_atmosphere"]
  }
}
```

---

## 三、输入素材体系

### 生成套图前需要收集的信息

#### A. 产品素材（必需）

| 素材 | 重要性 | 用途 | 缺失时的处理 |
|------|--------|------|------------|
| 产品白底图（正面） | ★★★ 必需 | Hero图的产品主体 | 无法生成，必须提供 |
| 产品多角度图（侧面/45度/俯视） | ★★☆ 强烈建议 | 细节图、参数图 | AI根据正面图推测其他角度 |
| 产品使用场景图 | ★☆☆ 加分项 | Lifestyle图的参考 | AI完全生成场景 |
| 产品细节/材质特写 | ★☆☆ 加分项 | Detail图的参考 | AI根据描述生成材质 |

#### B. 产品信息（必需）

| 信息 | 重要性 | 用途 |
|------|--------|------|
| 品牌名 | ★★★ | 品牌框、Logo |
| 产品名称 | ★★★ | 标题文案 |
| 核心卖点（3-8条） | ★★★ | 每张图的主题 |
| 价格区间 | ★★☆ | 决定定位感（高端/性价比） |
| 目标受众 | ★★☆ | 场景选择（户外人群/办公人群/家庭等） |
| 技术参数 | ★★☆ | 参数图 |
| 产品尺寸/重量 | ★☆☆ | 参数图 |
| 使用场景描述 | ★☆☆ | Lifestyle图 |

#### C. 竞品信息（可选但推荐）

| 信息 | 用途 |
|------|------|
| 竞品品牌和型号 | 对比图 |
| 竞品关键参数 | 参数对比（我们优势标✓，对手标✗） |
| 竞品Listing图片 | 风格参考、避免雷同 |
| 竞品价格 | 定位参考 |

#### D. 素材完整度分档

| 档位 | 条件 | 处理方式 |
|------|------|---------|
| **A档（完整）** | 至少1张清晰白底图 + 3条以上卖点 + 品牌名 | 直接走全套设计 |
| **B档（基本）** | 有产品图但缺场景图/细节图/参数信息 | 先做能做的，返回缺失清单让用户补 |
| **C档（最小）** | 只有品牌名和产品名称 | 搜索同类竞品作参考，纯AI生图，标记需人工确认 |

---

## 四、完整工作流

### 阶段一：素材评估（Gemini / Nano）

```
输入：用户提交的产品素材包（图片+信息+竞品）
      ↓
[Gemini 评估] ─→ 打分（产品可辨识性/卖点完整性/场景多样性/品牌元素）
      ↓
      ├── A档 → 进入"方案设计"
      ├── B档 → 返回缺失清单 → 用户补充 → 重新评估
      └── C档 → 启动网络寻图 → AI补全 → 标记为"需确认"
```

### 阶段二：方案设计（Gemini）

```
[Gemini 方案设计]
  ├── 2a. 提取产品主色调 + 确定整体风格
  │   └── 色调基准、氛围基准、排版风格、关联线索
  ├── 2b. 设计7张图的方案
  │   └── 每张图：类型、心理钩子、标题文案、推荐模型
  ├── 2c. 品牌框设计决策
  │   └── AI根据产品色调决定边框风格和配色
  └── 2d. 生成精细Prompt（每张图 × 每个模型的prompt）
```

### 阶段三：图片生成（多模型并行）

```
[按模型路由并行生成]
  ├── Flux 2 Pro → 写实场景（Hero/Lifestyle/Detail/Trust）
  ├── Ideogram V3 → 含文字的图（Benefit/Specs）
  ├── Nano Banana 2 → 快速版本（全部7张，用于快速预览）
  ├── Qwen-Image → 多语言版本（如果目标平台需要中文/俄语）
  └── Flux Schnell → 草稿版（调试prompt用）

  每张图同时生成 2-3 个模型版本 → 后续评审选最优
```

### 阶段四：AI 评审（Gemini Vision）

```
[Gemini Vision 自动评审]
  输入：生成的图片 + 原始prompt + 设计目标
  评分维度（每项1-10分）：
    1. Visual Quality（视觉质量）
    2. Product Representation（产品还原度）
    3. Mood/Atmosphere（氛围感）
    4. Text Rendering（文字质量，仅含文字的图）
    5. Composition（构图）
    6. Commercial Viability（商业可用性）
    7. Benchmark Proximity（与标杆的接近度）

  判定标准：
    ├── Overall ≥ 7 → ✅ 通过 → 进入品牌框合成
    ├── Overall 5-6 → ⚠️ 微调 → 走"Prompt微调"路径
    └── Overall < 5 → ❌ 重做 → 走"方案重设计"路径
```

### 阶段五：打回优化机制

```
不通过的图进入迭代循环：

[模式1: Prompt微调] — 问题小（光线/角度/细节偏差）
  ├── 保留原prompt 80%，调整问题部分
  ├── 保留原seed，只改关键参数
  ├── 重新生成1-2次
  └── 最多迭代3轮

[模式2: Prompt重写+反馈注入] — 问题中等（方向对但质量不达标）
  ├── 把AI评审反馈作为context喂给Gemini
  ├── Gemini重新设计这张图的prompt
  ├── 尝试换模型（Flux不行换Ideogram，或反过来）
  └── 最多迭代2轮

[模式3: 方案层面调整] — 问题大（这张图的概念就不对）
  ├── 回到阶段二，重新设计这张图的方案
  ├── 可能更换图片类型策略
  └── 需要人工介入决策

[模式4: 素材打回] — 素材本身有问题
  ├── 产品图模糊/角度不够 → 返回给用户要求补拍
  ├── 产品信息不完整 → 返回缺失项清单
  └── 竞品信息不足 → 自动搜索补充或跳过对比图
```

### 阶段六：品牌框合成 + 最终输出

```
[品牌框合成]
  ├── AI根据产品主色调生成品牌框（或使用LoRA训练的风格）
  │   └── EXCITAT 右上角、颜色与产品协调、风格匹配产品品类
  ├── 产品场景图 + 品牌框合成
  └── 输出 1200×1200 最终图

[人工终审]
  ├── 通过 → 输出最终套图
  ├── 微调 → 带人工意见回到阶段五
  └── 重做 → 带人工意见回到阶段二
```

---

## 五、当前状态与后续计划

### 当前（Phase 0）— 效果验证

- 用脚本（Node.js）调API逐一测试
- 验证各模型的生图质量和prompt有效性
- 手动跑流程，发现问题和瓶颈
- **产出：确认哪些模型好用、哪些prompt模板有效、品牌框方案是否可行**

### 后续（Phase 1）— 流程固化

- 把验证通过的流程写成 Skill
- 加入自动素材评估 → 方案设计 → 生成 → 评审的完整链路
- 品牌框LoRA训练（如果AI直接生成效果不够好）
- 建立prompt模板库（每种图片类型的最佳prompt）

### 远期（Phase 2）— 工作流自动化

- 迁移到 ComfyUI 节点工作流（可视化、可复用）
- Dify 作为上层编排（LLM决策 + 调用ComfyUI）
- 飞书多维表格集成（批量任务管理）
- 一个SKU从提交素材到输出7张图全自动
