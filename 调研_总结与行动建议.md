# Excitat AI 电商套图系统 · 终极调研总结与行动建议

> 综合五份方向调研（HuggingFace / ComfyUI / 即梦 Dreamina / 技术博客 / 国外同类项目）
> 输出日期：2026-04-15
> 定位回顾：**多品类通用框架**（speaker 是首个验证品类，非最终范围）/ fal.ai + Python Workflow + MongoDB / Amazon 8 图公式

---

## 一、10 个最值得立刻吸收的经验

### 1. 【来源 1/2/4】把"抠图-重光-合成-校色-锐化"拆成独立阶段，不要端到端硬怼
risunobushi v4（ComfyUI）、Tyler Ilunga 30 模型 pipeline、Photoroom multi-model architecture 都给了同一结论：电商图**不是一次生成 + 一次后修**，而是 BiRefNet → FLUX Kontext → IC-Light → Frequency Separation → Color Match → Upscale 这条串行流水线，每一步都由**最合适的专用模型**负责。
**落地**：Excitat 首版 Workflow 把 pipeline 至少拆 4 段：`cutout → compose → relight → retouch`，每段在 MongoDB 里独立存 artifact（可单独重跑，不必整链重算）。

### 2. 【来源 1】Finegrain Product Placement LoRA + FLUX Kontext 锁定为"产品置入场景"默认方案
这是目前唯一公开的 bbox-guided、自动重打光+透视+阴影的工业级开源 LoRA。对 8 图公式的"看场景""秀细节"格子直接命中。
**落地**：第一周就把权重上传 fal.ai LoRA hosting，在 `fal-ai/flux-kontext-lora` 端点上跑通 speaker 品类 3 个场景（露营/阳台/卧室），作为 MVP baseline。

### 3. 【来源 1/2】频率分离（Frequency Separation）保住 logo / 文字 / 按键纹理
risunobushi 的核心 trick：把原图 RGB 分离成 HF（高频细节）+ LF（低频色光），合成后只继承新图的 LF，HF 叠回原图。这是"AI 生成不糊文字"的通用解，对中国卖家带中文字卖点图尤其关键。
**落地**：在 Python Workflow 写个 `freq_separation(orig, generated)` 步骤（PIL Gaussian + Subtract 就够，不必依赖 ComfyUI 节点），默认开启。

### 4. 【来源 1/4】中文文案走 Qwen-Image-Edit，视觉编辑走 FLUX Kontext，双链路并存
FLUX 系列中文渲染基本不可用。Qwen-Image-Edit-2509 支持 1-3 张参考图 + product consistency，2511 更带自动 prompt 改写。
**落地**：MongoDB `recipes` 表里区分 `text_render_engine: qwen | flux`，8 图中"抓眼球""选规格""促下单"这三张带文字的默认走 Qwen，其余走 FLUX Kontext。

### 5. 【来源 2/3】"模式 D：Preset × Category 笛卡尔积"是最优雅的多品类架构
RunComfy 的 FLUX Kontext Preset Workflow（15+ 预设）和即梦"模板库 + Agent 扩写"殊途同归：**不做硬编码品类 classifier**，只做 (intent × category) 的预设矩阵。
**落地**：8 图公式 × 首批 3-5 品类 = 24-40 个预设文档。每个文档包含 prompt 骨架 + 参考图 URL + LoRA 栈 + 后处理参数。

### 6. 【来源 3】放弃"品类下拉"，改用"视觉模板库选一张像的"
即梦、Flair、Pebblely 都走这条路。用户认"图"不认"分类词"。
**落地**：Excitat 前端首页应该是 Pinterest 式样图瀑布流，每张图背后挂完整 recipe 文档，点击复用。

### 7. 【来源 4】CLIP 相似度 + 规则做批量 QA，是规模化的隐形护城河
Planningo 的教训：不同品类对"AI 幻觉"容忍度差异极大（服装 10%、硬件 1%）。人工过图在 100 SKU 以上就崩。
**落地**：MVP 就埋 `quality_gate(orig, generated) → {clip_similarity, text_preservation_score, aspect_ratio_check}`，低于阈值自动重试 + 打标上报。

### 8. 【来源 5】prompt pack / 场景预设库是核心资产，不是模型
levelsio 的 Photo AI 用 PHP + jQuery + Replicate API 做到 $150K/月，赢在 100+ style pack 的手工打磨。
**落地**：第一个月至少 70% 工时投在 speaker 品类的 prompt pack 打磨上（目标：8 图 × 5 种风格 = 40 个可复用单元），而不是架构优化。

### 9. 【来源 2】Impact Switch 按"后处理差异"而不是"品类名称"分支
ComfyUI 社区共识：分支颗粒度应该是 `硬质工业品 / 软质食品纺织 / 服装鞋包 / 家居大件` 四支，而不是按品类名罗列。
**落地**：MongoDB `categories` 表里存 `pipeline_branch: hard_goods | soft_goods | apparel | home_decor`，category 到 branch 是多对一映射。

### 10. 【来源 1/4】BiRefNet Dynamic 档 + Kontext 的 "subject consistency" 替代传统"生成+抠+贴"三段式
FLUX.2 + Kontext 已经能做到换场景时产品形态/LOGO/文字完整保留，很多"先抠后合成"的老 pipeline 可以被单次 Kontext 调用替代，省 60% 延迟和成本。
**落地**：speaker 品类先跑 A/B：(a) 传统三段式 (b) 单次 Kontext + BiRefNet 兜底。若 (b) 质量达标，默认切 (b)。

---

## 二、5 个需要避开的坑

### 坑 1：押单一大模型
**证据**：【来源 4】Photoroom、Flair、Claid 全部放弃单模型。Excitat 若只押 FLUX 2 Pro，一年内会被多模型架构拉开差距。
**对策**：底层至少预留 FLUX / Qwen / Kontext / BiRefNet / IC-Light 五条接入。

### 2：做"又一个 AI 产品图工具"
**证据**：【来源 5】Booth.ai 带 YC 背书仍在 2025-05 关停，死因是"市场同质化 + 产品力不够强"。
**对策**：差异化必须至少强 10 倍。Excitat 的差异化三板斧：(a) Amazon 8 图系统化而非散张，(b) 中国卖家 + 中文字符渲染，(c) Workflow 批量 + MongoDB 状态可追溯。

### 3：为每个 SKU 训 LoRA
**证据**：【来源 4】Basil Chatha 的成本实测——LoRA 对中小客单摊不下来（训练 $2-5 + 时间 20-40 分钟），只对高客单重点 SKU 划算。
**对策**：MVP 不做 SKU-level LoRA，只做品类级 style LoRA（可选）。Kontext + 参考图 + Finegrain LoRA 对 80% SKU 足够。

### 4：做 Freemium 带水印
**证据**：【来源 5】levelsio 明确拒绝 freemium，用"低门槛月订阅 + 积分"跑通 $150K MRR。freemium 引来的人 90% 不会付费，还拖慢 infra。
**对策**：$19/$39/$99 三档订阅 + 积分，不做免费带水印版。

### 5：硬编码品类 classifier
**证据**：【来源 3】即梦/Dreamina 本身没有品类识别器，走的是 Prompt Agent 扩写 + 视觉模板 + 参考图约束三件套。硬编码 classifier 会在新品类上线时被迫改代码。
**对策**：品类走"数据表驱动 + LLM 意图识别"，新品类冷启动只加数据行、不改代码。

---

## 三、3 个商业化启发

### 启发 1：定价 —— 订阅 + 积分双层，不搞 freemium
【来源 5】Photoroom/Claid/Pebblely 都是"免费 → $15 → $39 → Enterprise"四档，Photo AI 是 $29 无限 + 积分。**共性：中间档就是转化主力**。
**建议**：Excitat 定价 $19（100 图/月）/ $49（400 图/月）/ $149（2000 图/月）/ Enterprise API。首充送 50 图试用，不做永久免费。

### 启发 2：Wedge —— Amazon 中国卖家 + 8 图系统化切入
【来源 4/5】Claid 横打 marketplace、Photoroom 横打 C 端、levelsio 纵打单品类。Excitat 应该**纵打**：先在"Amazon 8 图 + 中国卖家 + 中文字"这个三重切片里做到第一，再横向扩品类。
**建议**：首版官网标题直接写 "Amazon 亚马逊 8 图公式 · AI 套图工作流 · 专为中国卖家优化"。别写"通用 AI 产品图工具"。

### 启发 3：获客 —— Build-in-Public + 客户在 r/FBA，不在 r/SideProject
【来源 5】levelsio 用 Twitter Build-in-Public 单条推带过单日 $7K MRR 增长。r/SideProject 里都是同行不是买家。
**建议**：创始人每周发 1 条 Twitter（MRR/本周 SKU 数/新 recipe），同时在 r/AmazonSeller + Shopify 中国跨境卖家微信群做 workflow 教学内容。ROI 完爆付费投放。

---

## 四、多品类框架设计启示（专章）

### 4.1 业界 category abstraction 的四种做法对比

| 模式 | 代表 | 机制 | 优点 | 缺点 | Excitat 采用建议 |
|---|---|---|---|---|---|
| **A. Prompt 模板变量** | cgtips、ComfyUI-Style-Selector、即梦模板库 | 同一 pipeline，换 prompt YAML | 零运维、可无限扩品类 | 无法解决品类间光影/构图差异 | **必用**（作为底层） |
| **B. Switch 条件分支** | Claid 按品类分 Recipe、MyAIForce V4 产品/人像分流 | `if branch == hard_goods: pipeline_A()` | 差异化充分 | 每加 branch 要测试 | **必用**（作为中层） |
| **C. LoRA 路由** | Lora Manager、Astria.ai | 动态加载品类 LoRA | 质量上限高 | 需要素材 + 训练成本 | **二期做**（验证后再上） |
| **D. Kontext 预设矩阵** | RunComfy FLUX Kontext Preset、即梦 Agent 扩写 | (intent × category) 笛卡尔积文档 | 最贴 8 图公式，subject consistency | 依赖 Kontext 模型能力 | **必用**（作为顶层路由） |

**Excitat 推荐组合 = A + B + D**：顶层 8 图 intent 预设（D）→ 中层 4 支 pipeline branch（B）→ 底层 prompt YAML（A）。MongoDB 存所有文档，Python Workflow 按 `(intent, category)` 查装配。

### 4.2 按品类预设 vs 全品类共用 —— 分类表

| 元素 | 按品类预设 | 全品类共用 |
|---|---|---|
| 主配色方案（王牌三色法：theme/font/bg）| ✅ 每品类 2-3 套预设 | — |
| 默认场景 prop 列表（露营椅/餐桌/地毯）| ✅ 每品类一张 prop 字典 | — |
| 光影预设（硬光/柔光/金色时刻/冷色调）| ✅ 按 `pipeline_branch` 分 | — |
| Prompt 风格形容词包（cinematic/minimal/editorial）| ✅ 品类专属 | — |
| 参考图（IPAdapter/Redux 注入）| ✅ 品类专属 | — |
| Allowed aspect ratio / 画幅 | ✅（食品常方形、服装常竖 3:4） | — |
| 抠图模型（BiRefNet Dynamic/Matting/Portrait）| ✅ 软货/发毛用 Matting | — |
| QA 阈值（CLIP 相似度下限、变形容忍度）| ✅ 硬件 0.92 / 服装 0.80 | — |
| 基础模型（FLUX Kontext / Qwen-Image-Edit / BiRefNet）| — | ✅ |
| Workflow 骨架（cutout→compose→relight→retouch）| — | ✅ |
| 频率分离后处理 | — | ✅ |
| Color Match 算法 | — | ✅ |
| Upscale 策略（Ultimate SD Upscale）| — | ✅ |
| Amazon 8 图槽位定义 | — | ✅ |
| MongoDB schema / Python Workflow 编排层 | — | ✅ |
| 品控模块（`quality_gate`） | — | ✅（阈值品类化） |

**原则**：**骨架共用、血肉品类化**。骨架改一次全品类受益，血肉是新品类冷启动时唯一要填的部分。

### 4.3 新品类冷启动 SOP（minimal engineering onboarding）

目标：**新增一个品类 = 加数据行，不改代码**。流程参考 Claid 和即梦的做法：

**Step 1：收集素材（半天工作量）**
- 10-20 张该品类的优秀真实产品图（用作参考）
- 5-10 张目标风格场景参考图（用作 Redux/IPAdapter 注入）
- 该品类的 3 套配色方案（theme + font + background，王牌三色法）

**Step 2：建 category 文档（1 小时）**
在 MongoDB `categories` 表插入：
```
{
  id: "camping_speaker",
  display_name: "营地灯音箱",
  pipeline_branch: "hard_goods",
  aspect_ratios: ["1:1", "4:3"],
  cutout_model: "birefnet_dynamic",
  qa_threshold: {clip: 0.92, text_preserve: 0.95},
  color_palettes: [...],
  props_dict: ["tent", "camping chair", "lantern", "pine wood table"],
  style_adjectives: ["rugged", "cinematic", "golden hour"],
  reference_images: [s3://...]
}
```

**Step 3：生成 8 个 recipe 文档（2 小时）**
笛卡尔积：8 图 intent × 该 category = 8 个 recipe（抓眼球/戳痛点/给方案/秀细节/看场景/建信任/选规格/促下单）。每个 recipe 是一段 Kontext prompt 骨架，填入 category 的 props/style/palette 变量。

**Step 4：跑 20 SKU 真机测试（半天）**
用 speaker 品类先行验证的 Python batch 脚本，跑 20 个 SKU 各 8 张 = 160 张图，走 `quality_gate`。通过率 >85% 即该品类上线。低于阈值的单元回到 Step 3 调 prompt，不改代码。

**Step 5：加入前端模板库（半小时）**
把最佳 20 张结果挂到前端"灵感广场"，用户可一键复用 recipe。

**总工时：1-2 人天 / 新品类**。这是 Claid 的真实节奏，也应该是 Excitat 的目标。

---

## 五、Excitat MVP 架构建议

### 5.1 技术栈决定树

```
需要做的事
├─ 图像生成/编辑
│   ├─ 主视觉：fal.ai `flux-kontext-lora` + Finegrain Product Placement LoRA
│   ├─ 中文文字：fal.ai `qwen-image-edit-2509/2511`
│   └─ 抠图：fal.ai `birefnet/v2 (Dynamic)`，兜底 Finegrain Object Cutter（自部署）
├─ 后处理
│   ├─ 频率分离：Python PIL/OpenCV 自写
│   ├─ Color Match：skimage.exposure.match_histograms
│   └─ Upscale：fal.ai `creative-upscaler` 或 Real-ESRGAN
├─ 编排
│   └─ Python Workflow（async，不上 Celery 先用 FastAPI BackgroundTasks）
├─ 存储
│   ├─ MongoDB：categories / recipes / prompts / quality_rules / jobs / artifacts
│   └─ S3/R2：图像 artifact
├─ 前端
│   └─ Next.js + 模板库 + 批量上传面板（参考 Photoroom Web UX）
└─ 品控
    └─ `quality_gate`：CLIP 相似度 + OCR 文本保留 + aspect 校验
```

**明确不做的**：不上 Celery/K8s、不训 SKU-level LoRA、不自建 GPU 集群、不 freemium。

### 5.2 核心数据表结构建议

```
categories {
  _id, display_name, pipeline_branch (enum: hard/soft/apparel/home),
  aspect_ratios[], cutout_model, text_render_engine,
  qa_threshold {clip, text_preserve, deformation_tolerance},
  color_palettes[] {theme, font, background},
  props_dict[], style_adjectives[], reference_image_urls[]
}

recipes {
  _id, category_id, intent (enum: eye_catch/pain/solution/detail/scene/trust/spec/cta),
  prompt_template, kontext_preset, lora_stack[],
  controlnet_config, redux_reference, post_processing_steps[],
  estimated_cost_usd, estimated_latency_s
}

prompts {
  _id, recipe_id, version, prompt_text, negative_prompt,
  variables{product_name, brand_color_hex, scene_tag, ...},
  tested_sku_count, success_rate, last_updated_at
}

quality_rules {
  _id, pipeline_branch, check_type (clip/ocr/aspect/deformation),
  threshold, fail_action (retry/flag/reject), max_retries
}

jobs {
  _id, user_id, sku_id, category_id, status, intent_grid[8],
  created_at, completed_at, total_cost_usd
}

artifacts {
  _id, job_id, intent, stage (cutout/compose/relight/retouch),
  s3_url, clip_score, gen_model, prompt_id, created_at
}
```

### 5.3 首批落地 Skill 清单（原子能力）

仿 Claid 的 skill 抽象方法，建议首批 7 个 skill（每个 = 一个 Python async 函数 + 一个 HTTP 端点）：

1. **`skill_cutout(image) → transparent_png`** — BiRefNet Dynamic 主链路 + Finegrain Object Cutter 兜底
2. **`skill_compose(product_png, scene_prompt, bbox) → composed_image`** — Finegrain Product Placement LoRA + FLUX Kontext
3. **`skill_relight(image, light_desc) → relit_image`** — FLUX Kontext prompt 重打光（MVP 不上自部署 IC-Light）
4. **`skill_text_overlay(image, cn_text, layout) → image_with_text`** — Qwen-Image-Edit-2509 渲染中文卖点
5. **`skill_retouch(orig, generated) → polished_image`** — 频率分离 + Color Match
6. **`skill_upscale(image, target_res) → hd_image`** — fal creative-upscaler
7. **`skill_quality_gate(orig, generated, category) → pass/fail + reasons`** — CLIP + OCR + aspect 检查

**Recipe 层**在 skill 之上做组合：`recipe_amazon_hero_shot(sku) = cutout → compose → relight → retouch → text_overlay → upscale → quality_gate`。

### 5.4 3 个月 Roadmap

**Month 1 · 单品类打穿（speaker）**
- Week 1: fal.ai 5 个端点接通 + MongoDB schema 落地 + 7 个 skill 骨架
- Week 2: Finegrain Product Placement LoRA 上 fal hosting，speaker 品类 3 场景 A/B
- Week 3: 8 图 recipe 全部跑通，`quality_gate` 阈值校准
- Week 4: 20 SKU 真机回归，前端 demo 页上线，创始人 Twitter 开始 Build-in-Public

**Month 2 · 横向验证（+食品 +宠物 +家居小件）**
- Week 5-6: 按 SOP 跑 3 个新品类冷启动，验证"加数据行不改代码"的承诺
- Week 7: 批量上传面板 + 用户账号 + Stripe 订阅（$19/$49/$149）
- Week 8: 灵感广场（Pinterest 式模板库）+ SEO 内容（Claid 式"如何给 X 品类做 Amazon 主图"系列）

**Month 3 · 规模化准备**
- Week 9-10: Webhook + API batch + 商家 feed 对接（抄 Claid）
- Week 11: 多品类 LoRA 路由（模式 C）小范围试上，给 Pro 档差异化
- Week 12: 首批付费用户案例研究 + r/AmazonSeller 内容投放 + MRR 复盘

**Month 3 末目标**：3-5 个品类上线、50 付费用户、$3-5K MRR、1-2 个公开案例研究、可批量跑 100 SKU/用户/月 的稳定 infra。

---

## 附：五份调研的一句话总结

- **方向 1 HuggingFace**：Finegrain Product Placement LoRA + BiRefNet + Qwen-Image-Edit + In-Context-LoRA 是主链路黄金组合，fal.ai 原生支持其中 3/4。
- **方向 2 ComfyUI**：频率分离 / Impact Switch / Kontext Preset / Dynamic Prompts 四件套可直接迁移到 Python Workflow，分支颗粒度按"后处理差异"切。
- **方向 3 即梦**：模型/产品分层解耦，品类走"模板 + LLM 扩写 + 参考图"三件套回避硬编码 classifier，多图层 + 边缘保持 + 融合按钮是电商刚需。
- **方向 4 技术博客**：多模型编排已成事实标准，FLUX.2+Kontext 是一致性新基准，品类分发必须是第一层路由，QA/后处理是真正护城河。
- **方向 5 同类项目**：Claid 是多品类 API 样板，Photoroom 是规模化样板，levelsio 是独立开发者样板，Booth.ai 关停是反面警告；核心资产是 prompt pack + Recipe 表，不是模型。
